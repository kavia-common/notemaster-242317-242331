# Notes Backend - environment variables
# These are expected to be provided by the notes_database container/runtime.

POSTGRES_URL=localhost
POSTGRES_PORT=5001
POSTGRES_USER=appuser
POSTGRES_PASSWORD=changeme
POSTGRES_DB=myapp
